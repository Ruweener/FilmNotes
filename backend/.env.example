MONGODB_URI=
THEMOVIEDB_API_KEY=
SUPABASE_URL=
# Use the publishable key for client-side (VITE) only
SUPABASE_PUBLISHABLE_KEY=
# Service role key MUST stay server-side and never be exposed to the frontend
SUPABASE_SERVICE_ROLE_KEY=
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
PORT=3000
NODE_ENV=development
